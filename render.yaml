services:
  - type: web
    name: github-stars-history-visualizer
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production

